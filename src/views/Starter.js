import { Card, CardBody, CardTitle } from "reactstrap";

const Starter = () => {
  return (
    <Card>
        <CardBody>
<CardTitle>Dashboard</CardTitle>
        </CardBody>
    </Card>
  );
};

export default Starter;
